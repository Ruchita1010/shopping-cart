import styles from './styles/Home.module.css';

const Home = () => {
  return (
    <div className={styles.home}>
      <p className={styles.tagline}>Step into adventure with</p>
      <p className={styles.tagline}>every stride</p>
    </div>
  );
};

export default Home;
